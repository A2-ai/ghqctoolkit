name: "GET /api/configuration/checklists"
description: "Successfully fetch configuration checklists"

fixtures:

git_state: {}

request:
  method: GET
  path: "/api/configuration/checklists"

response:
  status: 200
  body:
    match_type: schema
    schema:
      type: array
      item_fields:
        - name
        - content
      first_item:
        name: "Custom"
        content: "- [ ] [INSERT]"